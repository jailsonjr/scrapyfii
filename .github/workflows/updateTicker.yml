name: ticker-price-update
on: 
  push:
    branches:
      - main
  schedule:
    - cron: '* * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: run-job
        run: curl --request POST https://srbarrigafiis.vercel.app/api/ticker/update
